export com.pengyou.model.entity.Tag
    -> package com.pengyou.model.dto.tag

specification SearchForTags {
    id
    like/i(name)
    like/i(description)
}

SearchForTagsView {
    #allScalars(this)
}

specification TagForQuery {
    id
    name
}

TagForQueryView {
    #allScalars(this)
}